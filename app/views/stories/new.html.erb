<h1>Create your own story</h1> 

<%= form_with model: @story do |f| %>
    Select your name  <%= f.collection_select :writer_id, @writers, :id, :name, {prompt: true} %>
    
    or enter your name & age:

    <%= f.fields_for :writers do |writer| %> 
        <%= writer.text_field :name %>
        <%= writer.number_field :age %> 
    <% end %>
    <br>
    <br> 
    Who are you writing about? <%= f.text_field :subject %><br><br>

    What's their story: <%= f.text_field :content %><br><br>

    In which country ? <%= f.collection_select :country_id, @countries, :id, :name %><br><br>

    How would you categorize your story? <br><br>
        <% @tags.each do |tag| %>
            <%= f.check_box :tags, {:multiple => true}, tag.id %>
            <%= f.label tag.name %> 
        <%end%>
        <br>
        <br>
    <%= f.submit %>
<% end %>